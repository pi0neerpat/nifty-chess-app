# Migration `20210212091144-migration`

This migration has been generated by patrick at 2/12/2021, 4:11:44 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "AuthDetail" (
    "id" TEXT NOT NULL,
    "nonce" TEXT NOT NULL,
    "timestamp" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY ("id")
)

CREATE TABLE "User" (
    "id" TEXT NOT NULL,
    "authDetailId" TEXT NOT NULL,

    FOREIGN KEY ("authDetailId") REFERENCES "AuthDetail"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)

CREATE TABLE "Game" (
    "id" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "playedAt" DATETIME NOT NULL,
    "mintedAt" TEXT,
    "minterAddress" TEXT,
    "tokenId" INTEGER,
    "externalUrl" TEXT,
    "location" TEXT NOT NULL,
    "event" TEXT,
    "moves" TEXT NOT NULL,
    "moveCount" INTEGER NOT NULL,
    "black" TEXT NOT NULL,
    "white" TEXT NOT NULL,
    "winner" TEXT NOT NULL,

    FOREIGN KEY ("minterAddress") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("id")
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20210212091144-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,42 @@
+datasource DS {
+  // optionally set multiple providers
+  // example: provider = ["sqlite", "postgresql"]
+  provider = ["sqlite", "postgresql"]
+  url = "***"
+}
+
+generator client {
+  provider      = "prisma-client-js"
+  binaryTargets = "native"
+}
+
+model AuthDetail {
+  id        String   @id @default(uuid())
+  nonce     String
+  timestamp DateTime @default(now())
+}
+
+model User {
+  id    String  @id
+  authDetail AuthDetail
+  games Game[] @relation(name: "gamesMinted" )
+}
+
+model Game {
+  id String   @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  playedAt DateTime
+  mintedAt String?
+  minter User?  @relation(name: "gamesMinted", fields: [minterAddress], references: [id])
+  minterAddress String?
+  tokenId Int?
+  externalUrl String?
+  location String
+  event String?
+  moves String
+  moveCount Int
+  black String
+  white String
+  winner String
+}
```


