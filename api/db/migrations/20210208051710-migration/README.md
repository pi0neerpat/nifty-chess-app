# Migration `20210208051710-migration`

This migration has been generated by patrick at 2/8/2021, 12:17:10 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
DROP INDEX "User.address_unique"

PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Game" (
    "id" TEXT NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" DATETIME NOT NULL,
    "playedAt" DATETIME NOT NULL,
    "mintedAt" DATETIME NOT NULL,
    "minterAddress" TEXT,
    "tokenId" INTEGER,
    "externalUrl" TEXT,
    "location" TEXT NOT NULL,
    "event" TEXT,
    "moves" TEXT NOT NULL,
    "black" TEXT NOT NULL,
    "white" TEXT NOT NULL,
    "whiteWins" BOOLEAN NOT NULL,

    FOREIGN KEY ("minterAddress") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_Game" ("id", "createdAt", "updatedAt", "playedAt", "mintedAt", "moves", "black", "white") SELECT "id", "createdAt", "updatedAt", "playedAt", "mintedAt", "moves", "black", "white" FROM "Game";
DROP TABLE "Game";
ALTER TABLE "new_Game" RENAME TO "Game";
CREATE TABLE "new_User" (
    "id" TEXT NOT NULL,
    "authDetailId" TEXT NOT NULL,

    FOREIGN KEY ("authDetailId") REFERENCES "AuthDetail"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    PRIMARY KEY ("id")
);
INSERT INTO "new_User" ("id", "authDetailId") SELECT "id", "authDetailId" FROM "User";
DROP TABLE "User";
ALTER TABLE "new_User" RENAME TO "User";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210207202517-migration..20210208051710-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,40 +1,41 @@
 datasource DS {
   // optionally set multiple providers
   // example: provider = ["sqlite", "postgresql"]
-  provider = "sqlite"
-  url = "***"
+  provider = ["sqlite", "postgresql"]
+  url = "***"
 }
 generator client {
   provider      = "prisma-client-js"
   binaryTargets = "native"
 }
-// Define your own datamodels here and run `yarn redwood db save` to create
-// migrations for them.
-// TODO: Please remove the following example:
-model User {
-  id    Int     @id @default(autoincrement())
-  address String @unique
-  authDetail AuthDetail
-}
-
 model AuthDetail {
   id        String   @id @default(uuid())
   nonce     String
   timestamp DateTime @default(now())
 }
+model User {
+  id    String  @id
+  authDetail AuthDetail
+  games Game[] @relation(name: "gamesMinted" )
+}
+
 model Game {
-  id        String   @id @default(uuid())
+  id String   @id
   createdAt DateTime @default(now())
   updatedAt DateTime @updatedAt
   playedAt DateTime
   mintedAt DateTime
+  minter User?  @relation(name: "gamesMinted", fields: [minterAddress], references: [id])
+  minterAddress String?
+  tokenId Int?
+  externalUrl String?
+  location String
+  event String?
   moves String
-  movesHash String
   black String
   white String
-  minter User
-
+  whiteWins Boolean
 }
```


